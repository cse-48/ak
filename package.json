{
  "name": "ak",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/cse-48"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cse-48/ak.git"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  },
  
  "devDependencies": {
    "lerna": "^5.3.0",
    "typescript": "^4.7.4"
  }
}
